{
  "name": "task-board",
  "version": "1.0.0",
  "description": "Trello-style task board monorepo — root scripts exist so Railway's builder can build and start the app",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install --include=dev",
    "start": "cd backend && npm start"
  }
}
